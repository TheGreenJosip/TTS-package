SPEECH_REGION=
SPEECH_KEY=

# Trigger word which needs to be right before the text to be spoken
TRIGGER_WORD=TTS
# Port where the app receives inputt via API
PORT=4753

# Optional: queue hardening
MAX_QUEUE_LENGTH=100
TTS_MAX_RETRIES=2
TTS_RETRY_BASE_DELAY_MS=500
